# Change build and source for RIDUX integration

* Remove modules we don't use to simplify build
* Add Sp interface
* Update bundle Import- and Export-Package
* Close sctp socket when stopping the Stack
